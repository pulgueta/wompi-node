# Wompi SDK documentation

This app uses [Blume](https://useblume.dev). Documentation lives in `content/`.

```sh
pnpm --filter docs dev
```

Run the full documentation gate with:

```sh
pnpm --filter docs build
```
